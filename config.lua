--+==========================================================+--
--|          ESX Minecraft Inspired Crafting System          |--
--|                        by Akkariin                       |--
--+==========================================================+--

Config = {
    debug     = false,   -- Enable debug mode?
    keybind   = 'f10',   -- Keybind to open/close the crafting table
    locale    = 'zh-CN', -- Locale to use, allow: en-US, zh-CN
    enableKey = true,    -- Enable keybind?
    enableCmd = true,    -- Enable command?
    legacyEsx = true,    -- Use legacy esx? (legacy: using limit; new: using weight)
    maxCraft  = 9999999, -- Max craft amount
}
